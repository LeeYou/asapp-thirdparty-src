# boost license placeholder

`Boost` 已纳入 `third_party/manifests/dependencies.yaml`，当前作为 `app-service` transport 的受控依赖候选。

当前阶段仅约束 `Boost.Asio + Boost.Beast` 子集。

后续在正式纳管源码时，应同步补齐上游许可证文本，并纳入发布阶段的 `THIRD_PARTY_NOTICES` 与 `SBOM` 生成流程。
